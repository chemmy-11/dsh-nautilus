# @dsh-external/dsh-nexus bundle patch 层。
# 装配：`dsh plugin --profile <name> add github:chemmy-11/dsh-nautilus`（或 npm/tarball）。
- insert:
    - id: nexus
      name: '@dsh-external/dsh-nexus'
    # Nautilus OS/GPU 层（Phase 1）：独立插件入口，与 nexus 同包不同行、同库不同表。
    - id: pulse
      name: '@dsh-external/dsh-nexus/pulse'
